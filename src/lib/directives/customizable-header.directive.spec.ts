import { CustomizableHeaderDirective } from './customizable-header.directive';

describe('CustomizableHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomizableHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
