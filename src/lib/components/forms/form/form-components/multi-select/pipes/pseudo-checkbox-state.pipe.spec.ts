import { PseudoCheckboxStatePipe } from './pseudo-checkbox-state.pipe';

describe('PseudoCheckboxStatePipe', () => {
    it('create an instance', () => {
        const pipe = new PseudoCheckboxStatePipe();
        expect(pipe).toBeTruthy();
    });
});
