import { NumberHelper } from '../number.helper';

describe('Helper: NumberHelper', () =>
{
    it('should round a given number', () =>
    {
        // expect(NumberHelper.round).toBeDefined();
    });
});