import { TerraFormHelper } from './terra-form.helper';

describe(`TerraFormHelper:`, () =>
    {
        let helper:TerraFormHelper;

        beforeEach(() =>
            {
                helper = new TerraFormHelper();
            });

        it('should', function()
        {

        });
    }
)
